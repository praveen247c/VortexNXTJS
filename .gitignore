/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local
.vercel

# logs
*.log

# local archives
*.zip

# MCP registry auth — private signing key must never be committed
key.pem
*.pem
